# Workizo Workspace Rules

## Git Policy
- **Do not push directly to the `main` branch.**
- Always create a new feature branch (e.g., `feat/...` or `fix/...`) before making changes.
- Push changes to the feature branch so they can be merged via Pull Requests.
